

## This pass

Describe this change. Do not judge it.

Call set_overview once and describe_file once for every file on the list
below, every one of them and nothing outside it. Call add_cohort only if this
pass asks for cohorts. Make no add_comment call. A later call
over this same material writes the comments, so a defect you notice here is
that call's to report. The context blocks above are there for
that call.