---
phase: 01-briefing-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - internal/packet/ingest_test.go
autonomous: true
requirements:
  - AGT-01
  - AGT-03
  - INT-01
  - INT-04

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "intent as a string or moved as a string fails ParseReview with an error containing review is not valid JSON (D-10, AGT-01)"
    - "An extra unknown JSON key beside summary succeeds; Summary is set; Review still has no second headline field (D-01, D-10, INT-01)"
    - "Missing PR and ticket is not an error; summary-only and intent-without-links parse (D-05, INT-04)"
    - "Merge of next with empty Intent and empty Surfaces structs keeps previous values (D-09, AGT-01)"
    - "cmdIngest still parses stdin, merges, and writes; no generation or HTTP client on that path (D-11, AGT-03)"
    - "Existing files, screenshots, fence, and findings ingest tests still pass (ROADMAP success 4)"
  artifacts:
    - internal/packet/ingest_test.go
  key_links:
    - "ParseReview type errors → wrapped UnmarshalTypeError"
    - "Empty Intent{} / Surfaces{} → intentEmpty / surfacesEmpty → keep prev"
---

## Phase Goal

**As a** driving agent, **I want to** send why the change exists and one line per surface on ingest, **so that** a second ingest that omits those fields does not blank them.

## Acceptance Criteria

- [ ] Malformed intent or moved types fail closed (D-10)
- [ ] Unknown extra keys are ignored; headline stays summary (D-01, INT-01)
- [ ] Omitted PR or ticket does not fail parse (D-05, INT-04)
- [ ] Empty intent/surfaces objects keep previous values (D-09, AGT-01)
- [ ] Ingest still never calls a model; existing walkthrough/screenshots/findings still work (D-11, AGT-03)

<objective>
Expand the 01-01 tracer with fail-closed parse, omitted-link success, empty-object keep, and proof the ingest path still has no model client.

Purpose: Comment-loop agents omit keys or send empty objects; bad types must not persist; extra keys must not become a second headline (D-01, D-05, D-09, D-10).
Output: New parse/merge tests in ingest_test.go; existing packet and cmd/redline suites green.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-briefing-contract/01-CONTEXT.md
@.planning/phases/01-briefing-contract/01-RESEARCH.md
@.planning/phases/01-briefing-contract/01-PATTERNS.md
@.planning/phases/01-briefing-contract/01-01-SUMMARY.md
@internal/packet/ingest.go
@internal/packet/ingest_test.go
@cmd/redline/main.go

<interfaces>
After 01-01, ingest.go exports Intent, IntentPR, IntentTicket, Surfaces, Surface on Review and Merge empty-keeps via intentEmpty / surfacesEmpty.

ParseReview still: json.Unmarshal into Review; errors wrap "review is not valid JSON".
cmdIngest (cmd/redline/main.go): packet.ParseReview(os.Stdin) then packet.Merge(res.Review, rev). Target.Matches is CLI --pr vs session only — do not compare intent.pr to packet target (D-03).

Test style from ingest_test.go: package packet, strings.NewReader, t.Fatal. Reject pattern from target_test.go: err == nil || !strings.Contains(err.Error(), substring) then t.Fatalf.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Fail closed on bad types; ignore extra keys; omitted links succeed</name>
  <files>internal/packet/ingest_test.go</files>
  <read_first>internal/packet/ingest.go, internal/packet/ingest_test.go, .planning/phases/01-briefing-contract/01-PATTERNS.md, .planning/phases/01-briefing-contract/01-01-SUMMARY.md</read_first>
  <behavior>
    - TestParseReviewRejectsInvalidIntentType: raw JSON with summary and intent as the string PR #1 returns an error whose Error() contains review is not valid JSON (D-10)
    - TestParseReviewRejectsInvalidMovedType: raw JSON with surfaces.interface.moved as the string yes returns the same wrapped error (D-10)
    - TestParseReviewIgnoresUnknownGoal: JSON with summary x plus an extra unknown key succeeds; Summary equals x; there is no second headline field to read (D-01, D-10, INT-01)
    - TestParseReviewAllowsOmittedIntentLinks: JSON with only summary, and JSON with intent object and neither pr nor ticket, both succeed; missing links are not an error (D-05, INT-04)
  </behavior>
  <action>
    Add the four tests to ingest_test.go next to TestParseReviewFiles. Use strings.Contains on err.Error() for the two reject tests with substring review is not valid JSON. For the extra-key test, send summary x plus a goal key (not a Review field; D-01 forbids adding one). Assert parse succeeds and Summary is x. For omitted links, assert Intent may be nil or have nil PR and nil Ticket, and that err is nil.

    Do not enable unknown-field rejection on the decoder (D-10). Do not add a second headline field on Review (D-01). Do not edit ingest.go unless a test reveals 01-01 Unmarshal behavior is wrong — then only fix types, not ParseReview control flow. Do not add HTML or cmd/redline assertions.
  </action>
  <verify>
    <automated>go test ./internal/packet/ -run 'TestParseReviewRejectsInvalidIntentType|TestParseReviewRejectsInvalidMovedType|TestParseReviewIgnoresUnknownGoal|TestParseReviewAllowsOmittedIntentLinks|TestParseReviewIntentAndSurfaces|TestParseReviewToleratesFence' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/packet/ -run TestParseReviewRejectsInvalidIntentType -count=1 exits 0
    - go test ./internal/packet/ -run TestParseReviewRejectsInvalidMovedType -count=1 exits 0
    - go test ./internal/packet/ -run TestParseReviewIgnoresUnknownGoal -count=1 exits 0
    - go test ./internal/packet/ -run TestParseReviewAllowsOmittedIntentLinks -count=1 exits 0
    - TestParseReviewIgnoresUnknownGoal asserts Summary is x on success (INT-01, D-01)
    - Reject tests require strings.Contains(err.Error(), "review is not valid JSON") (D-10)
  </acceptance_criteria>
  <done>Bad types fail closed; extra keys are ignored; missing PR/ticket is not an error (INT-01, INT-04, D-01, D-05, D-10).</done>
</task>

<task type="auto" tdd="true">
  <name>Empty intent and surfaces objects keep previous; ingest path stays model-free</name>
  <files>internal/packet/ingest_test.go</files>
  <read_first>internal/packet/ingest.go, internal/packet/ingest_test.go, cmd/redline/main.go, .planning/phases/01-briefing-contract/01-PATTERNS.md</read_first>
  <behavior>
    - TestMergeKeepsEmptyIntentAndSurfaces: prev has filled Intent (PR+ticket) and Surfaces; next has Intent: and Surfaces: empty struct pointers; got keeps prev Intent and Surfaces (D-09, AGT-01)
    - Existing cmd/redline ingest tests still pass; cmdIngest still calls packet.ParseReview then packet.Merge (D-11, AGT-03)
  </behavior>
  <action>
    Add TestMergeKeepsEmptyIntentAndSurfaces in the same style as TestMergeKeepsOmittedNarrative. next must set Intent to a new empty Intent and Surfaces to a new empty Surfaces (not nil — that case is already the omit path). Assert previous objects survive. Do not add a partial-intent-keeps-ticket case (D-09).

    Then run the full packet package and cmd/redline tests. Confirm cmdIngest still goes ParseReview → Merge → write. Do not add a generation or HTTP client, do not GET intent URLs, do not compare intent.pr to the session PR target (D-03, D-04, D-11). Do not edit html, skills, DefaultGuidance, or packet.Version. If empty-struct keep fails, fix intentEmpty / surfacesEmpty in ingest.go so empty structs count as empty, then re-run.
  </action>
  <verify>
    <automated>go test ./internal/packet/ ./cmd/redline/ -count=1</automated>
    <human-check>This phase has no briefing page. After the automated suite is green, confirm git status shows no report HTML, skill, or packet.Version edits.</human-check>
  </verify>
  <acceptance_criteria>
    - go test ./internal/packet/ -run TestMergeKeepsEmptyIntentAndSurfaces -count=1 exits 0
    - go test ./internal/packet/ -count=1 exits 0
    - go test ./cmd/redline/ -count=1 exits 0 (AGT-03)
    - cmd/redline/main.go still contains packet.ParseReview and packet.Merge inside cmdIngest (D-11)
    - internal/packet/packet.go still contains `const Version = 1` (D-10)
  </acceptance_criteria>
  <done>Empty briefing objects keep previous values; ingest still has no model client; existing file/screenshot/findings ingest still works (AGT-01, AGT-03, D-09, D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent stdin → ParseReview | Untrusted JSON; type confusion is the primary attack |
| Intent URL strings | Must remain data, never fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ParseReview invalid intent / moved | high | mitigate | This plan's reject tests lock fail-closed UnmarshalTypeError wrapping (D-10) |
| T-01-03 | Information disclosure | Intent URLs | high | mitigate | Store only; cmdIngest gains no HTTP GET of those fields (D-04) |
| T-01-04 | Elevation of privilege | ingest path | high | mitigate | cmd/redline tests stay green; no generation client added (D-11, AGT-03) |
| T-01-06 | Tampering | unknown extra keys | medium | mitigate | Extra keys ignored; do not switch on unknown-field rejection (D-10, D-01) |
| T-01-SC | Tampering | package installs | low | accept | No new modules this phase |

</threat_model>

<verification>
- [ ] go test ./internal/packet/ -count=1
- [ ] go test ./cmd/redline/ -count=1
- [ ] All new reject / ignore / omit / empty-object tests green
- [ ] No report HTML, skill, or Version edits
</verification>

<success_criteria>
- Invalid types fail closed (D-10)
- Extra keys ignored; summary remains the headline (INT-01, D-01)
- Missing PR/ticket is not an error (INT-04, D-05)
- Empty objects keep previous intent/surfaces (AGT-01, D-09)
- Binary still makes no model calls (AGT-03, D-11)
</success_criteria>

## Artifacts this phase produces

Created in this plan:

- `TestParseReviewRejectsInvalidIntentType`
- `TestParseReviewRejectsInvalidMovedType`
- `TestParseReviewIgnoresUnknownGoal`
- `TestParseReviewAllowsOmittedIntentLinks`
- `TestMergeKeepsEmptyIntentAndSurfaces`

Created in 01-01 (do not recreate; tests consume them):

- `type Intent`, `type IntentPR`, `type IntentTicket`, `type Surfaces`, `type Surface`
- `Review.Intent`, `Review.Surfaces`
- `func intentEmpty`, `func surfacesEmpty`

<output>
Create `.planning/phases/01-briefing-contract/01-02-SUMMARY.md` when done
</output>
