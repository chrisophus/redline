---
phase: 01-briefing-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/packet/ingest.go
  - internal/packet/ingest_test.go
autonomous: true
requirements:
  - AGT-01
  - INT-01
  - INT-02
  - INT-03

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ParseReview accepts summary plus optional intent.pr {number,title,url}, intent.ticket {id,title,url}, and surfaces.interface/api/schema {line,moved} (D-01, D-02, D-04, D-06, INT-01, INT-02, INT-03)"
    - "Merge of a findings-only next Review keeps previous intent and surfaces, same as summary and files (D-09, AGT-01)"
    - "A non-empty supplied intent or surfaces object replaces the entire previous object, like files (D-09, AGT-01)"
    - "Idle surface {line set, moved false} is kept; {moved false} with empty line is treated as empty and does not wipe previous (D-06, D-09)"
    - "Headline field remains summary; Review has no second headline field (D-01, INT-01)"
    - "apiChanges, schemaChanges, and screenshots remain on Review and still empty-keep (D-07)"
    - "packet.Version stays 1; ParseReview still uses encoding/json Unmarshal (D-10)"
  artifacts:
    - internal/packet/ingest.go
    - internal/packet/ingest_test.go
  key_links:
    - "Review.Intent / Review.Surfaces → ParseReview Unmarshal → Merge intentEmpty/surfacesEmpty → session.Review persist (existing)"
---

## Phase Goal

**As a** driving agent, **I want to** send why the change exists and one line per surface on ingest, **so that** a second ingest that omits those fields does not blank them.

## Acceptance Criteria

- [ ] Ingest JSON can include the existing `summary` headline plus optional `intent.pr`, optional `intent.ticket`, and one `{line,moved}` each for `interface` / `api` / `schema` (D-01, D-02, D-04, D-06)
- [ ] A findings-only follow-up Merge keeps previous intent and surfaces (D-09, AGT-01)
- [ ] Non-empty supplied intent or surfaces replace the whole previous object (D-09)

<objective>
Wire the briefing ingest contract end-to-end: types → ParseReview → Merge omit-keep, then whole-object replace when the next review speaks.

Purpose: Comment-loop ingest must be able to carry PR/ticket/surface lines without blanking them on a findings-only follow-up. Phase 2 reads these fields; this plan only persists them (D-08).
Output: `Intent` / `Surfaces` types on `Review`, empty-keep helpers in `Merge`, extended parse + omit-keep + replace tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-briefing-contract/01-CONTEXT.md
@.planning/phases/01-briefing-contract/01-RESEARCH.md
@.planning/phases/01-briefing-contract/01-PATTERNS.md
@internal/packet/ingest.go
@internal/packet/ingest_test.go
@internal/target/target.go
@internal/packet/packet.go

<interfaces>
From internal/packet/ingest.go (grow in place; do not add a second DTO):

type Review struct {
  Summary string `json:"summary"`
  Files []FileNote `json:"files,omitempty"`
  APIChanges []Highlight `json:"apiChanges,omitempty"`
  SchemaChanges []Highlight `json:"schemaChanges,omitempty"`
  Findings []Judgment `json:"findings,omitempty"`
  Unknowns []string `json:"unknowns,omitempty"`
  Screenshots []Shot `json:"screenshots,omitempty"`
}

func ParseReview(r io.Reader) (*Review, error)  // json.Unmarshal; unknown keys ignored; type errors wrap "review is not valid JSON"
func Merge(prev, next *Review) *Review          // out := *next; empty string/slice copies prev
func (r *Review) ToFindings() []findings.Finding
func Apply(rep *findings.Report, rev *Review)

From 01-RESEARCH.md recommended shapes (JSON keys locked; Go names are discretion, use these):

type Intent struct { PR *IntentPR `json:"pr,omitempty"`; Ticket *IntentTicket `json:"ticket,omitempty"` }
type IntentPR struct { Number int `json:"number,omitempty"`; Title string `json:"title,omitempty"`; URL string `json:"url,omitempty"` }
type IntentTicket struct { ID string `json:"id,omitempty"`; Title string `json:"title,omitempty"`; URL string `json:"url,omitempty"` }
type Surfaces struct { Interface *Surface `json:"interface,omitempty"`; API *Surface `json:"api,omitempty"`; Schema *Surface `json:"schema,omitempty"` }
type Surface struct { Line string `json:"line,omitempty"`; Moved bool `json:"moved"` }

Empty: Intent nil or both PR and ticket empty; IntentPR nil or Number==0 && Title=="" && URL==""; IntentTicket nil or all strings empty; Surfaces nil or all three empty; Surface nil or Line=="" && !Moved.
A surface with a quiet line and Moved false is not empty. A surface with Moved true and empty Line is not empty.

From internal/target/target.go: PullRequest.Number is int. Do not copy Target.PR into Review.Intent (D-03).
From internal/packet/packet.go: const Version = 1. Do not change it (D-10).
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>End-to-end "send briefing fields, findings-only follow-up keeps them" — one path only</name>
  <reversibility rating="costly">D-02 / D-06 / D-09 become the published ingest contract skills will emit and comment-loop merge depends on; undo is a coordinated schema change.</reversibility>
  <files>internal/packet/ingest.go, internal/packet/ingest_test.go</files>
  <read_first>internal/packet/ingest.go, internal/packet/ingest_test.go, .planning/phases/01-briefing-contract/01-CONTEXT.md, .planning/phases/01-briefing-contract/01-RESEARCH.md, .planning/phases/01-briefing-contract/01-PATTERNS.md, internal/target/target.go, internal/packet/packet.go</read_first>
  <behavior>
    - TestParseReviewIntentAndSurfaces: JSON with summary, intent.pr {number:1, title, url}, intent.ticket {id:REL-24, title, url}, surfaces.interface {line, moved:true}, surfaces.api {quiet line, moved:false}, surfaces.schema {quiet line, moved:false}, and an apiChanges highlight parses without error; fields populate; moved false is preserved; Summary is the headline (INT-01, INT-02, INT-03, D-01, D-02, D-04, D-06, D-07)
    - TestMergeKeepsOmittedNarrative (extend, do not replace): prev carries the same filled Intent (PR+ticket) and Surfaces (at least one tile with line + moved); next is findings-only; got.Intent and got.Surfaces survive; existing summary/files/highlights/shots/findings asserts still hold (AGT-01, D-09)
  </behavior>
  <action>
    Write the two failing tests first (same package packet, stdlib testing, ParseReview via strings.NewReader, t.Fatal / t.Fatalf). Do not add a second keep-narrative test name. Fixture keys must be interface / api / schema, not sketch ui / db.

    Then grow Review in ingest.go: after Summary insert pointer fields Intent *Intent with tag intent omitempty and Surfaces *Surfaces with tag surfaces omitempty (D-02, D-06). Do not add a second headline field (D-01, INT-01). Do not remove or rename APIChanges, SchemaChanges, or Screenshots (D-07).

    Add types next to FileNote / Highlight using the RESEARCH shapes in context: Intent, IntentPR (Number int to match target.PullRequest.Number), IntentTicket (ID string, not Number), Surfaces with three pointer fields, Surface with Line omitempty and Moved tagged moved with no empty-omit flag so false survives marshal (locked discretion). Do not share one link struct. Do not use a string-keyed map for surfaces.

    Add unexported empty helpers beside Merge: intentEmpty, surfacesEmpty, and nested helpers as needed, using the empty rules in context. Idle copy with a non-empty Line and Moved false is not empty. Moved true with empty Line is not empty. Moved false with empty Line is empty so it cannot wipe previous (D-09).

    In Merge, after the Summary empty-keep, add: if intentEmpty(out.Intent) { out.Intent = prev.Intent }; if surfacesEmpty(out.Surfaces) { out.Surfaces = prev.Surfaces }. Keep start-from-*next. Do not empty-keep Findings. Do not deep-merge pr vs ticket.

    Leave ParseReview, Apply, and ToFindings untouched so new fields ride existing Unmarshal (D-10) and findings still stamp source llm (D-11). Do not copy packet target PR into Intent (D-03). Do not invent surface lines when the key is absent (D-08). Do not fetch intent URLs. Do not add a generation or HTTP client (D-11, AGT-03). Do not edit packet.Version, html/markdown/report templates, skills, DefaultGuidance, cmd/redline, or session.go.
  </action>
  <verify>
    <automated>go test ./internal/packet/ -run 'TestParseReviewIntentAndSurfaces|TestMergeKeepsOmittedNarrative|TestParseReviewFiles|TestParseReviewScreenshots|TestToFindingsStampsLLM|TestMergeHandlesNils' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/packet/ -run TestParseReviewIntentAndSurfaces -count=1 exits 0
    - go test ./internal/packet/ -run TestMergeKeepsOmittedNarrative -count=1 exits 0
    - internal/packet/ingest.go contains `Intent   *Intent` or `Intent *Intent` and `json:"intent,omitempty"` (D-02)
    - internal/packet/ingest.go contains `Surfaces *Surfaces` or `Surfaces   *Surfaces` and `json:"surfaces,omitempty"` (D-06)
    - internal/packet/ingest.go contains `func intentEmpty` and `func surfacesEmpty` (D-09)
    - internal/packet/ingest.go contains `json:"moved"` on Surface and does not attach an empty-omit flag to that tag (D-06 discretion)
    - internal/packet/packet.go still contains `const Version = 1` (D-10)
    - ParseReview still calls json.Unmarshal (D-10)
    - go test ./internal/packet/ -run 'TestParseReviewFiles|TestParseReviewScreenshots|TestToFindingsStampsLLM' -count=1 exits 0 (D-07)
  </acceptance_criteria>
  <done>One JSON path with summary + intent.pr + intent.ticket + three surfaces parses, and a findings-only Merge keeps those objects plus existing narrative fields (AGT-01, INT-01, INT-02, INT-03, D-01 through D-11 on this path).</done>
</task>

<task type="auto" tdd="true">
  <name>Non-empty intent and surfaces replace the previous objects</name>
  <reversibility rating="costly">D-09 whole-object replace matches files; a later deep-merge would be a new merge story.</reversibility>
  <files>internal/packet/ingest_test.go, internal/packet/ingest.go</files>
  <read_first>internal/packet/ingest.go, internal/packet/ingest_test.go, .planning/phases/01-briefing-contract/01-PATTERNS.md</read_first>
  <behavior>
    - TestMergePrefersTheNewReviewWhereItSpeaks (extend, do not rename): when next supplies non-empty Intent and Surfaces, got uses next's objects entirely, including dropping prev ticket if next intent has only PR (whole-object replace, like Files). Existing summary/files replace asserts still hold (AGT-01, D-09)
  </behavior>
  <action>
    Extend TestMergePrefersTheNewReviewWhereItSpeaks only. Put a filled Intent (PR + ticket) and Surfaces on prev. Put a different non-empty Intent (PR number 2 is enough) and a different non-empty Surfaces on next. Assert got.Intent and got.Surfaces equal next's values, not prev's. Keep the existing summary/files asserts.

    Do not add a test that sends a partial next Intent and expects prev ticket to survive — that would lock a deep-merge D-09 forbids. If Merge does not already whole-replace, fix only the empty helpers / Merge branches in ingest.go; do not invent a per-key merge.
  </action>
  <verify>
    <automated>go test ./internal/packet/ -run 'TestMergePrefersTheNewReviewWhereItSpeaks|TestMergeKeepsOmittedNarrative|TestMergeHandlesNils' -count=1</automated>
  </verify>
  <acceptance_criteria>
    - go test ./internal/packet/ -run TestMergePrefersTheNewReviewWhereItSpeaks -count=1 exits 0
    - go test ./internal/packet/ -run TestMergeKeepsOmittedNarrative -count=1 still exits 0
    - TestMergePrefersTheNewReviewWhereItSpeaks asserts next Intent and Surfaces win when non-empty (D-09, AGT-01)
  </acceptance_criteria>
  <done>Supplied non-empty intent and surfaces replace the previous objects the same way files do (D-09, AGT-01).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent stdin → ParseReview | Untrusted review JSON crosses into typed Review |
| Merge prev/next | Session-stored Review combined with new untrusted Review |
| Review.Intent URL fields | Agent-supplied strings; must not be fetched |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | ParseReview / Intent | high | mitigate | Typed structs + existing json.Unmarshal; type mismatch returns error wrapping review is not valid JSON. Plan 01-02 adds the reject tests. |
| T-01-02 | Tampering | Surfaces | medium | mitigate | Struct with three fixed keys, not a string-keyed map, so sketch ui/db keys cannot silently bind (D-06) |
| T-01-03 | Information disclosure | Intent.PR.URL / Intent.Ticket.URL | high | mitigate | Store only. Do not GET those URLs. No ticket-host client (D-04, D-11) |
| T-01-04 | Elevation of privilege | cmdIngest / packet | high | mitigate | No generation client on the ingest path (D-11, AGT-03). Findings stay stamped llm via existing ToFindings |
| T-01-05 | Tampering | report HTML | low | accept | XSS via summary/line is Phase 2; this plan does not edit html/template |
| T-01-SC | Tampering | package installs | low | accept | No new modules this phase |

</threat_model>

<verification>
- [ ] go test ./internal/packet/ -count=1
- [ ] TestParseReviewIntentAndSurfaces, TestMergeKeepsOmittedNarrative, TestMergePrefersTheNewReviewWhereItSpeaks green
- [ ] Existing fence/files/screenshots/ToFindings/nil-merge tests still green
- [ ] packet.Version remains 1
- [ ] No edits to html.go, markdown.go, report templates, skills, or cmd/redline
</verification>

<success_criteria>
- Agent JSON can carry intent and surfaces through ParseReview (INT-01, INT-02, INT-03)
- Findings-only Merge keeps those fields (AGT-01, D-09)
- Non-empty next intent/surfaces replace previous (AGT-01, D-09)
- Headline remains summary (D-01)
</success_criteria>

## Artifacts this phase produces

Phase-wide (this plan creates the production symbols; 01-02 adds more tests):

- `type Intent` — `internal/packet/ingest.go`
- `type IntentPR` — fields `Number int`, `Title string`, `URL string`
- `type IntentTicket` — fields `ID string`, `Title string`, `URL string`
- `type Surfaces` — fields `Interface`, `API`, `Schema` as `*Surface`
- `type Surface` — fields `Line string`, `Moved bool`
- `Review.Intent *Intent` — JSON key `intent`
- `Review.Surfaces *Surfaces` — JSON key `surfaces`
- `func intentEmpty(*Intent) bool`
- `func surfacesEmpty(*Surfaces) bool`
- nested empty helpers if added (`intentPREmpty`, `intentTicketEmpty`, `surfaceEmpty`)
- `TestParseReviewIntentAndSurfaces`
- extensions to `TestMergeKeepsOmittedNarrative`
- extensions to `TestMergePrefersTheNewReviewWhereItSpeaks`

<output>
Create `.planning/phases/01-briefing-contract/01-01-SUMMARY.md` when done
</output>
