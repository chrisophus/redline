# .github/redline-post.yml

# Required, both of them. post refuses to load a profile without them.
# They are hidden markers a merge gate reads, so if nothing is gating on
# your reviews the names are yours to pick.
review_marker: redline-review:v1
finding_marker: redline-finding:v1

# The setting you want. "evidence" is the default and gives the table.
body_style: walkthrough

# Optional extras in the walkthrough. Drop the ones you don't want.
# intent and evidence are left out: the pull request already shows its
# description. coverage, lint, confirmations and unknowns are left out too.
body_include:
  - line-counts       # +/- beside each file and on each group heading
  - test-totals       # test files as a heading with totals, no rows
  - generated-totals  # one heading with the generated files' totals
  - low-confidence    # unsure and hedged findings, folded instead of withheld
  - diff-links        # each file links to its diff, except tests
